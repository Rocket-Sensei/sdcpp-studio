models:
  chroma-unlocked:
    enabled: false
    name: "chroma unlocked v10 Q8_0"
    quant: "Q8_0"
    description: "chroma unlocked v10 Q8_0"
    capabilities: ["text-to-image"]
    supports_negative_prompt: false
    command: "./bin/sd-server"
    args:
      - "--diffusion-model"
      - "./models/chroma/chroma-unlocked-v10-Q8_0.gguf"
      - "--vae"
      - "./models/vae/flux1_f32.safetensors"
      - "--clip_l"
      - "./models/clips/clip_l.safetensors"
      - "--t5xxl"
      - "./models/clips/t5xxl_fp16.safetensors"
      - "-v"
      - "--offload-to-cpu"
      - "--clip-on-cpu"
      - "--steps"
      - "24"
      - "--chroma-disable-dit-mask"
    mode: "on_demand"
    exec_mode: "server"
    # Port is dynamically allocated (40000-49000 range)
    model_type: "text-to-image"
    default_size: "1024x1024"
    startup_timeout: 120000
    generation_params:
      cfg_scale: 1.0
      sample_steps: 24
      sampling_method: "euler"
