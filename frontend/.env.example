# Frontend Development Server Configuration
# Server host - use 0.0.0.0 to listen on all interfaces (for remote access)
HOST=0.0.0.0

# Allowed hosts for Vite dev server (for proxy domains and custom hostnames)
# - Set to 'all' to allow any host (useful for development with proxy domains)
# - Set to comma-separated list of specific hosts: 'example.com,.example.com,localhost'
# - A leading dot (e.g., '.example.com') allows all subdomains
# - Default: 'localhost,.localhost' (allows localhost and its subdomains)
ALLOWED_HOSTS=all

# Backend server for API proxy (used in development)
BACKEND_HOST=127.0.0.1
BACKEND_PORT=3000
